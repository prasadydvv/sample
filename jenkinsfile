#edited
pipeline {
    agent { docker { image 'node:16.13.1-alpine' } }
    stages {n
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
    }
}
end this script you delete this line the only you execute this code
